{
  "name": "sas-sports-cloudflare",
  "version": "3.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node tests/regression.mjs",
    "test": "node tests/regression.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
